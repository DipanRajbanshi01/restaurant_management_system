const express = require('express');
const router = express.Router();
// const { createItem, getItems, getItemById, updateItem, deleteItem } = require('../controllers/itemController');
// const { auth, authorize } = require('../middleware/auth');

// TODO: Implement item routes
// router.post('/', auth, authorize('admin'), createItem);
// router.get('/', getItems);
// router.get('/:id', getItemById);
// router.put('/:id', auth, authorize('admin'), updateItem);
// router.delete('/:id', auth, authorize('admin'), deleteItem);

module.exports = router;

